# Module 6 Micro-Closure Specification — Formal Definitions & Constitutional Freeze

**Module:** Module 6 — Classical-to-Quantum Expressibility & Equivalence Analysis  
**Stage:** Micro-Closure  
**Status:** FORMALLY CLOSED / FROZEN  
**Preconditions:** Modules 1, 2, 3, 4, 5 are FORMALLY COMPLETE / FROZEN  

---

## 1. Frozen Primary Definitions

1. **Classical Algorithm Domain ($A_C$):**  
   Frozen as $A_C = A_\text{semantic}$ where each element is a finite-domain transition system $(D_\text{fin}, R_P)$ with $D_\text{fin} \subset C_R$ ($|D_\text{fin}| < \infty$) and $R_P: D_D \to D_\text{fin}$ derived from Module 1 AML/RUTM programs. $A_C$ is NOT arbitrary string syntax or unrestricted infinite algorithms.

2. **Quantum Circuit Domain ($C_Q$):**  
   Frozen as $C_Q = C_Q^\text{logical}$ (logical `QuantumCircuitIR` AST space from Module 4 output contract).

3. **Compiler Mapping ($F$):**  
   Frozen as the compiler-induced mapping $F: A_C \to C_Q^\text{logical}$ generated by the Module 1–4 compilation pipeline ($\text{AML/RUTM} \to \text{RUTM-IR} \to \text{QTM-IR} \to \text{QuantumCircuitIR}$).

4. **Classical Semantic Equivalence ($\equiv_C$):**  
   Frozen as Transition Equivalence ($\equiv_\text{transition}$) over finite domain $D_\text{fin}$:
   $$A_1 \equiv_C A_2 \iff R_{P1}(x) = R_{P2}(x) \quad \forall x \in D_\text{fin}$$

5. **Quantum Semantic Equivalence ($\equiv_Q$):**  
   Frozen as Operator Equivalence ($\equiv_\text{operator}$) up to workspace ancilla uncomputation:
   $$Q_1 \equiv_Q Q_2 \iff U_{Q1} |x\rangle |0_A\rangle = U_{Q2} |x\rangle |0_A\rangle \quad \forall x \in D_\text{fin}$$

6. **Ancilla Policy:**  
   Workspace ancillas $A$ are distinct from logical history $H$. Semantic equivalence requires clean ancilla uncomputation ($|0_A\rangle \to |0_A\rangle$). Circuits leaving prohibited garbage in workspace ancillas are semantically INVALID.

7. **Global Phase Policy:**  
   Exact basis equality ($U_C |E(C)\rangle = |E(R_P(C))\rangle$) is enforced where exact semantics are required. Operator comparison may recognize global phase factor $e^{i\phi}$ when evaluating $U_1 \equiv_Q U_2$.

8. **Finite-Domain Boundary:**  
   All current analytical bounds are restricted to finite $D_\text{fin}$ ($|D_\text{fin}| < \infty$). Promoting finite-domain results to infinite state spaces without formal proof is strictly FORBIDDEN.

9. **Quotient Mapping ($\bar{F}$):**  
   Frozen as an ANALYTICAL TARGET $\bar{F}: A_C / \equiv_C \to C_Q / \equiv_Q$. Well-definedness ($A_1 \equiv_C A_2 \implies F(A_1) \equiv_Q F(A_2)$) is an unproven research target.

10. **Image ($\text{Img}(F)$):**  
    Frozen as an ANALYTICAL OBJECT $\text{Img}(F) = \{F(A) \mid A \in A_C\} \subseteq C_Q^\text{logical}$.

11. **Open Hypotheses & Unproven Properties:**  
    - Injectivity: UNPROVEN (Research Property).
    - Surjectivity: UNPROVEN (Research Property).
    - Hadamard Non-Surjectivity ($H \notin \text{Img}(F)$): OPEN HYPOTHESIS.

---

## 2. Claim-vs-Evidence Classification Matrix

| Statement | Classification | Evidence / Basis |
|---|---|---|
| $A_C$ Definition | **DEFINITION** | Finite transition system $(D_\text{fin}, R_P)$ |
| $C_Q$ Definition | **DEFINITION** | Module 4 `QuantumCircuitIR` AST space |
| $F$ Definition | **CONTRACT / VERIFIED** | Module 1–4 compiler synthesis pipeline |
| Classical Equivalence $\equiv_C$ | **DEFINITION** | Transition Equivalence $\equiv_\text{transition}$ |
| Quantum Equivalence $\equiv_Q$ | **DEFINITION** | Operator Equivalence $\equiv_\text{operator}$ |
| Ancilla Uncomputation | **CONTRACT** | Module 4 $|0_A\rangle \to |0_A\rangle$ invariant |
| Global Phase Policy | **CONTRACT** | Module 4 exact basis / Module 5 phase equivalence |
| $F$ Injectivity | **UNPROVEN PROPERTY** | Research Target for Stage 4 |
| $F$ Surjectivity | **UNPROVEN PROPERTY** | Research Target for Stage 5 |
| Hadamard $H \notin \text{Img}(F)$ | **OPEN HYPOTHESIS** | Research Target for Stage 6 |
